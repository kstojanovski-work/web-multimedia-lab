services:
  redis:
    image: redis:7-alpine
    container_name: text-to-sign-redis
    ports:
      - "6379:6379"
    command: ["redis-server", "--save", "60", "1", "--loglevel", "warning"]
    restart: unless-stopped
